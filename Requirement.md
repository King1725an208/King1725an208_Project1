# 知识树平台 · 需求文档

| 项目 | 内容 |
|---|---|
| 文档版本 | v2.0（平台化大改，取代 v1.0） |
| 日期 | 2026-08-15 |
| 状态 | 已与需求方逐条确认（grilling 会话：第 1~2 轮 Q1–Q10 + 第 3 轮 R3-Q1~Q4 全部定案） |
| 内容输入源 | 作者 Word 文档（首个主题：《鲁国_孔子_溯源.docx》） |
| 交付物 | ① `index.html`（知识树引擎，内嵌 Confucius 示例主题包）② `SCHEMA.md`（主题包格式说明）③ `Requirement.md`（本文档） |

---

## 0. v2.0 变更说明

v1.0 定位是"一个孔子主题的网页知识树"。v2.0 应需求方要求升级为**平台**：

1. **通用引擎 + 可插拔内容**：知识树引擎（渲染/交互/编辑）与内容数据（各主题知识）彻底解耦；孔子只是首个示例主题，后续可制作其他主题的知识树（如老子、苏轼、任意家族史/学科脉络）。
2. **标准内容格式**：Word 文档仅为输入源，必须转换为"知识树主题包"标准 JSON 格式（见 `SCHEMA.md`）后方可被引擎加载。
3. 第 1~2 轮的旧决策按第 3 轮确认重新分层：引擎相关升级为通用能力，Confucius 特有内容降级为数据集（见 §12 决策记录）。

## 1. 项目概述

一个**离线单文件的知识树平台**：以"树的高度 = 时间"为核心可视化隐喻，把任意按时间演化的知识主题渲染为一棵可缩放、可交互、可编辑的知识树。平台自带首个主题《我心中的孔子》（前551–2026，孔子的真实生平 → 被记录 → 被传播 → 我接受的），并支持导入其他主题包扩展。

## 2. 目标与受众（Q1）

- **受众**：需求方自我整理 + 给孩子/家人讲解；具备公开分享的展示品质。
- **Confucius 主题文风**：保留 docx 第一人称随笔温度；金句保留《论语》原文并配口语化解读。
- **平台文风**：引擎本身不含任何主题观点，全部文案由主题包数据驱动。

## 3. 平台架构（R3-Q1）

```
┌─────────────────────────────────────────────┐
│ index.html（单文件，离线双击即开）             │
│  ├─ 引擎层：渲染 / 交互 / 编辑 / 持久化 / 主题  │
│  └─ 内嵌数据区：Confucius 示例主题包（JSON）    │
└─────────────────────────────────────────────┘
        ↑ 导入主题包 JSON（文件选择器，无 fetch）
        ↓ 导出主题包 JSON（备份/分享/固化）
```

- **引擎单文件**：HTML/CSS/JS + 内嵌 D3.js；不依赖任何网络资源（用户网络走代理，file:// 下 fetch 本地文件会被浏览器拦截，故采用文件选择器导入方案）。
- **数据可插拔**：新主题 = 一个符合 `SCHEMA.md` 的 JSON 文件，导入即用，无需改引擎代码。
- **Pages-ready**：文件结构保证日后开 GitHub Pages 可直接访问（Q10）。

## 4. 内容格式与转换（R3-Q2）

- **交付 `SCHEMA.md`**：定义"知识树主题包"标准格式——主题元信息（id/title/简介/作者/版本）、时间轴配置（起止年份）、章节/泳道定义（id/名称/颜色）、枝干角色（root 根部 / trunk 主干 / branch 侧枝 / canopy 树梢）、节点（id/标题/年份/所属章/摘要/原文节选/金句/人物注/子节点）、导航结构。
- **转换流程**：Word 文档 →（本次由开发者手工转换）→ Confucius 主题包 JSON（内嵌于引擎 + 可导出为独立文件）；后续新主题由需求方按 SCHEMA.md 编写，或交给开发者转换。
- **引擎校验**：导入时校验 JSON 是否符合 schema，不符合则提示具体缺失字段。

## 5. 引擎功能需求（通用，所有主题共享）

| 编号 | 需求 | 来源 |
|---|---|---|
| FR-E01 | **数据驱动渲染**：按主题包数据渲染单时间主轴知识树；章节配色、枝干角色、导航结构、时间轴范围全部来自数据，引擎无主题硬编码 | R3-Q4 |
| FR-E02 | **首屏全局骨架视图**：整树自适应缩放到一屏可见，节点显示标题，详情收起 | Q9 |
| FR-E03 | **缩放/拖拽平移**：D3 zoom，惯性顺滑，双击复位 | Q5 |
| FR-E04 | **节点交互**：点击展开/收起详情卡；子树折叠/展开 | Q5 |
| FR-E05 | **搜索过滤**：关键词高亮匹配节点并聚焦定位 | Q5 |
| FR-E06 | **导航跳转**：按主题包导航结构生成跳转菜单，平滑定位 | Q5 |
| FR-E07 | **详情卡渲染器**：支持 schema 全部可选字段（年份/章色标/摘要/原文节选/金句卡/人物注），按数据存在与否渲染，缺字段不报错 | Q8 + R3-Q4 |
| FR-E08 | **页内编辑模式**：开关切换；可修改节点文字、增删子节点、调整年份 | Q2+ |
| FR-E09 | **编辑持久化**：localStorage 自动保存，按主题包 id 隔离，刷新/重开不丢；可"恢复内置版本" | Q2+ |
| FR-E10 | **主题包导入/导出**：文件选择器导入 JSON（schema 校验）；一键导出当前主题包 JSON | R3-Q1 |
| FR-E11 | **主题切换器**：顶栏下拉列出全部已加载主题（内置 + 已导入），点击切换；可移除已导入主题 | R3-Q3 |
| FR-E12 | **视觉三主题**：水墨国风（默认）/ 现代简约 / 深色暗夜，一键切换并持久化选择 | Q7 |
| FR-E13 | **定稿固化**：导出的 JSON 可替换 HTML 内嵌数据块，形成新的内置版本用于发布 | Q2+ |

## 6. Confucius 数据集需求（首个主题包）

| 编号 | 需求 | 来源 |
|---|---|---|
| FR-D01 | **骨架忠实 docx**：四章（真实→记录→传播→接受）+ 传播十阶段，章节四色区分 | Q3 |
| FR-D02 | **枝干映射**：真实=根部幼苗段（生平节点：出生前551、三岁丧父、问学、办学、55岁周游列国十四年、归鲁、前479逝世）；记录=《论语》/六经侧枝；传播=主干十阶段；接受=树梢（含"如果孔子还活着"尾声） | Q3 |
| FR-D03 | **时间锚点**：节点按阶段等距排列（顺序保真），标注真实年份（前551–前479、约前134、1905、2026 等） | Q2 / Q4 |
| FR-D04 | **人物背景注**：董仲舒、朱熹、王阳明、康有为等关键人物附一两句身份说明 | Q2 |
| FR-D05 | **金句卡**：《论语》原文 + 口语化解读 | Q1 / Q8 |
| FR-D06 | **文风**：第一人称随笔温度，docx 原文节选入详情卡 | Q1 / Q8 |

## 7. 非功能需求

| 编号 | 需求 |
|---|---|
| NFR-01 | **单文件零运行时依赖**：引擎 + 内嵌 D3.js（约 +280KB）于 `index.html`，离线双击即开；构建时 D3 下载失败则降级手写实现（Q6） |
| NFR-02 | **丝滑**：动画/缩放/平移目标 60fps，transform + rAF（Q5） |
| NFR-03 | **中文排版**：系统字体栈（宋体系标题 + 清晰正文），不加载网络字体 |
| NFR-04 | **兼容性**：现代桌面浏览器（Chrome/Edge 优先），移动端基础可浏览 |
| NFR-05 | **引擎/数据分离**：主题包为独立 JSON（内嵌 `<script>` 数据块），可整体替换；引擎代码中不得出现任何 Confucius 具体内容 |

## 8. 视觉主题（引擎内置，Q7）

| 主题 | 要点 |
|---|---|
| 水墨国风（默认） | 宣纸/米白底、墨色枝干、朱砂红点缀、宋体标题 |
| 现代简约 | 纯白底、单一主题色、无衬线字体 |
| 深色暗夜 | 墨黑/深蓝底、金色/荧光节点 |

章节配色由主题包数据定义，引擎保证在三套视觉主题下均可读可区分。

## 9. 数据与持久化

- **三层数据**：① 内嵌内置主题包（随文件分发）② localStorage 编辑态数据（按主题 id 隔离）③ 导入的主题包（存 localStorage，可移除）。
- 加载优先级：localStorage 编辑态 > 内置版本；存在编辑态时提示可"恢复内置版本"。
- 链路：页内编辑 → localStorage → 导出 JSON → 固化回 HTML 内嵌数据区（FR-E13）。

## 10. 交付与发布（Q10）

- 交付至本仓库（`King1725an208_Project1`）根目录：`index.html`、`SCHEMA.md`、`Requirement.md`。
- 现阶段 git 存档，**不开** GitHub Pages；结构 Pages-ready。

## 11. 验收标准

1. 双击 `index.html` 离线打开，首屏一屏览尽 Confucius 全树骨架。
2. 树结构符合 FR-D01/D02：根部生平 → 侧枝《论语》/六经 → 主干传播十阶段 → 树梢当代，真实年份齐全。
3. 点击节点弹出详情卡，字段按数据渲染（Confucius 主题六字段齐全）。
4. 缩放/拖拽/折叠/搜索/导航跳转流畅可用。
5. 三套视觉主题可切换且刷新后保持。
6. 编辑模式修改后刷新仍在；导出 JSON 可重新导入。
7. **平台性验证**：构造第二个符合 SCHEMA.md 的最小主题包（如 3 节点的"老子"示例），导入后引擎无需改代码即可正确渲染、切换、编辑。
8. 导入一个故意缺字段的 JSON，引擎给出明确的校验错误提示而非白屏。
9. `SCHEMA.md` 与实际数据格式一致，可被第三方（或未来的需求方）按文档独立写出合法主题包。
10. git 提交完成，根目录可见 `index.html`、`SCHEMA.md`、`Requirement.md`。

## 12. 决策记录

### 第 1~2 轮（v1.0，Q1–Q10）

| # | 决策点 | 定案 | v2.0 处理 |
|---|---|---|---|
| Q1 | 受众与文风 | 个人随笔风格 + 可公开品质 | 文风部分 → Confucius 数据集（FR-D06）；品质要求 → 平台级 |
| Q2 | 内容边界 | docx 为主 + 时间锚点 + 人物注 | → Confucius 数据集（FR-D03/D04）；"Word 仅作输入源" → 平台级（§4） |
| Q2+ | 编辑维护 | 页内编辑 + localStorage + 导入导出 + 固化 | → 引擎（FR-E08/09/10/13） |
| Q3 | 骨架 | 单时间主轴；真实=根/记录=侧枝/传播=主干/接受=树梢；四章色分 | 单轴+枝干角色机制 → 引擎；四章映射 → 数据集（FR-D01/D02） |
| Q4 | 时间比例 | 阶段等距 + 真实年份标注 | 布局机制 → 引擎；年份内容 → 数据集 |
| Q5 | 交互 | 高交互丝滑 | → 引擎（FR-E03/04/05/06 + NFR-02） |
| Q6 | 技术 | 单文件 + 内嵌 D3 | → 平台级（NFR-01） |
| Q7 | 视觉 | 三主题可切换，默认水墨 | → 引擎（FR-E12） |
| Q8 | 详情卡 | 全量六字段 | 渲染器能力 → 引擎（FR-E07）；实际字段 → 数据集 |
| Q9 | 首屏 | 全局骨架视图 | → 引擎（FR-E02） |
| Q10 | 发布 | 先存档不开 Pages，Pages-ready | → 平台级（§10） |

### 第 3 轮（v2.0 平台化转向，R3-Q1~Q4）

| # | 决策点 | 定案 |
|---|---|---|
| R3-Q1 | 架构形态 | 引擎单文件 + 内置 Confucius 示例 + 导入主题 JSON + 页内主题切换 |
| R3-Q2 | 内容格式 | 交付 SCHEMA.md；本次 docx 由开发者手工转换；后续主题按 schema 编写或交开发者转换 |
| R3-Q3 | 多主题管理 | 顶栏轻量主题切换器，可移除已导入主题 |
| R3-Q4 | 泛化划分 | 渲染/交互/编辑/视觉主题为引擎通用；章节/枝干/节点/导航/时间轴为数据声明 |

## 13. 约束与假设

- 不引入任何 CDN/外部网络资源（离线可用）。
- Confucius 内容版权为需求方自有（docx 原创随笔）。
- 单主题节点量约 30+，无需虚拟化渲染；引擎按单主题 ≤500 节点设计余量。
- 浏览器 file:// 限制：不依赖 fetch/XHR 加载本地文件，导入一律走文件选择器。